<div class="row">
    <div class="col-md-12">
        <h2>Employees</h2>
        <hr />
        <div class="table-responsive">
            <table class="table">
                <thead>
                    <tr>
                        <td>Department Number</td>
                        <td>Department Name</td>
                    </tr>
                </thead>
                <tbody>
                    {{#each departments}}
                        <tr>
                            <td><a href="/employees?department={{this.departmentId}}">{{departmentId}}</a></td>
                            <td><a href="/employees?department={{this.departmentId}}">{{departmentName}}</a></td>
                        </tr>
                    {{else}}
                            <tr>
                            <td colspan="4">No Data Available</td>
                        </tr>
                    {{/each}}
                </tbody>
            </table>
        </div>
    </div>
</div>